pipeline {
    agent { 
        label 'master'
        }
    stages {
         stage("Helm install")
             steps {
                   echo "=====================Helm instal================"
                      {it -> sh " helm install Chart/ "  
                      }
                   }   
                }
             }   
